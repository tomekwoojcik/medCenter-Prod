import React from 'react'
import { NavLink } from 'react-router-dom'

const UserData = () => {
    return (
        <div>


        </div>
    )
}

export default componentName



{ localStorage.user !== null || "" ? <NavLink to="/user" /> : <NavLink to="/userData" /> }
